apiVersion: v1
kind: Service
metadata:
  name: {{ include "nictiz-ui.fullname" . }}
  labels:
    {{- include "nictiz-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nictiz-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
